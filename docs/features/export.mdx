---
title: "Export Options"
description: "Export presentations as PPTX, PDF, or images"
---

## Formats

| Format | Description |
|--------|-------------|
| PPTX | Slides are embedded as images in the PPT file. You can play and reorder pages in PowerPoint, but the text inside images cannot be directly edited |
| Editable PPTX (Beta) | SVG-route projects are translated into native PowerPoint shapes and text boxes |
| PDF | Pixel-perfect output, ready for presentation |
| Images | Each page exported as an individual image file |

## PPTX Export Panel

Regular PPTX export now opens a settings panel before generating the file. You can enable slide transitions and choose classic effects such as Fade, Page Turn, Push, Wipe, Split, Blinds, Checkerboard, and Clock. When multiple effects are selected, each slide randomly uses one of the selected effects.

## Aspect Ratio

Defaults to 16:9. You can change the ratio when creating a project or in **Project Settings** on the preview page. Supports 16:9, 4:3, 21:9, 1:1, 9:16, and more. Images must be regenerated after changing the ratio.

## Editable PPTX (Beta)

Editable PPTX export is available for projects generated through the SVG route. The stored SVG for each page is translated directly into native PowerPoint shapes, text boxes, and images. Image-route reverse engineering with OCR, MinerU, or inpainting is no longer supported.

## Page Selection

You can select specific pages to export rather than exporting the entire presentation. Enable **Multi-select** on the Slide Preview page, check the pages you want, then click **Export**.

## Allow Partial Results

The **Allow Partial Results** option in **Project Settings → Export Settings** now applies consistently across export flows.

- When disabled: the export fails immediately on critical gaps or errors, such as missing generated slides or editable PPTX conversion failure.
- When enabled: the system will continue whenever possible and return partial output if some pages cannot be exported.
